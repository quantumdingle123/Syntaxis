-- game lock check
local targetGame = 70845479499574
if game.PlaceId ~= targetGame and game.GameId ~= targetGame then
    pcall(function()
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Syntax Hub",
            Text = "Wrong game bro!",
            Duration = 5
        })
    end)
    return 
end

-- file saving and config stuff
local http = game:GetService("HttpService")
local saveFile = "SyntaxHub_Config.json"

local settings = {
    Key = "",
    -- We only need to load the key for the loader
}
local currentSettings = settings

local function saveStuff()
    if writefile then
        pcall(function() writefile(saveFile, http:JSONEncode(currentSettings)) end)
    end
end

local function loadStuff()
    if isfile and readfile and isfile(saveFile) then
        pcall(function()
            local decoded = http:JSONDecode(readfile(saveFile))
            if decoded and decoded.Key then
                currentSettings.Key = decoded.Key
            end
        end)
    end
end
loadStuff() 

local me = game:GetService("Players").LocalPlayer
local myGui = me:WaitForChild("PlayerGui")
local ts = game:GetService("TweenService")

-- key system ui setup
local keyScreen = Instance.new("ScreenGui")
keyScreen.Name = "SyntaxKeyScreen"
keyScreen.ResetOnSpawn = false
keyScreen.DisplayOrder = 999999999

local keyBg = Instance.new("Frame")
keyBg.Size = UDim2.new(0, 320, 0, 220)
keyBg.Position = UDim2.new(0.5, -160, 0.5, -110)
keyBg.BackgroundColor3 = Color3.fromRGB(15, 20, 30)
keyBg.BorderSizePixel = 0
keyBg.Parent = keyScreen
Instance.new("UICorner", keyBg).CornerRadius = UDim.new(0, 12)
local keyStroke = Instance.new("UIStroke", keyBg); keyStroke.Color = Color3.fromRGB(42, 78, 132); keyStroke.Thickness = 2

local titleTxt = Instance.new("TextLabel", keyBg); titleTxt.Size = UDim2.new(1, 0, 0, 30); titleTxt.Position = UDim2.new(0, 0, 0, 5); titleTxt.BackgroundTransparency = 1; titleTxt.Text = "Syntax Hub"; titleTxt.TextColor3 = Color3.fromRGB(240, 248, 255); titleTxt.Font = Enum.Font.GothamBold; titleTxt.TextSize = 18
local subTxt = Instance.new("TextLabel", keyBg); subTxt.Size = UDim2.new(1, 0, 0, 20); subTxt.Position = UDim2.new(0, 0, 0, 30); subTxt.BackgroundTransparency = 1; subTxt.Text = "Join our Discord to get a key!"; subTxt.TextColor3 = Color3.fromRGB(180, 210, 235); subTxt.Font = Enum.Font.Gotham; subTxt.TextSize = 12
local box = Instance.new("TextBox", keyBg); box.Size = UDim2.new(0, 280, 0, 40); box.Position = UDim2.new(0, 20, 0, 60); box.BackgroundColor3 = Color3.fromRGB(25, 30, 45); box.TextColor3 = Color3.fromRGB(255, 255, 255); box.PlaceholderText = "Redeem key"; box.Text = ""; box.Font = Enum.Font.Gotham; box.TextSize = 14
Instance.new("UICorner", box).CornerRadius = UDim.new(0, 8)
local boxStroke = Instance.new("UIStroke", box); boxStroke.Color = Color3.fromRGB(42, 78, 132); boxStroke.Thickness = 1
local checkBtn = Instance.new("TextButton", keyBg); checkBtn.Size = UDim2.new(0, 280, 0, 40); checkBtn.Position = UDim2.new(0, 20, 0, 110); checkBtn.BackgroundColor3 = Color3.fromRGB(42, 78, 132); checkBtn.TextColor3 = Color3.fromRGB(255, 255, 255); checkBtn.Font = Enum.Font.GothamBold; checkBtn.TextSize = 14; checkBtn.Text = "Verify Key"
Instance.new("UICorner", checkBtn).CornerRadius = UDim.new(0, 8)

local discordBtn = Instance.new("TextButton", keyBg); discordBtn.Size = UDim2.new(0, 280, 0, 40); discordBtn.Position = UDim2.new(0, 20, 0, 160); discordBtn.BackgroundColor3 = Color3.fromRGB(88, 101, 242); discordBtn.TextColor3 = Color3.fromRGB(255, 255, 255); discordBtn.Font = Enum.Font.GothamBold; discordBtn.TextSize = 14; discordBtn.Text = "Join Discord"
Instance.new("UICorner", discordBtn).CornerRadius = UDim.new(0, 8)

-- ==========================================
-- JNKIE API KEY AUTHENTICATION
-- ==========================================
local discordLink = "https://discord.gg/xRUfWZz4gk"

local Junkie = nil
pcall(function()
    Junkie = loadstring(game:HttpGet("https://jnkie.com/sdk/library.lua"))()
    Junkie.service = "Syntax"
    Junkie.identifier = "1073462"
    Junkie.provider = "LootLabs"
end)

local function verifyWithJNKie(inputKey)
    if not Junkie then return false, "" end
    local cleanKey = string.gsub(inputKey, "%s+", "")
    if cleanKey == "" then return false, "" end
    
    local success, result = pcall(function() return Junkie.check_key(cleanKey) end)
    if success and result and result.valid then return true, cleanKey end
    return false, cleanKey
end

local function executeMainHub()
    -- !!! PASTE YOUR OBFUSCATED JNKIE LOADSTRING RIGHT BELOW THIS LINE !!!
    
    loadstring(game:HttpGet("https://api.jnkie.com/api/v1/luascripts/public/42035527fd272c07577ad2bed86ccdd61c429b77e77bb59963f886c7681c9f78/download"))()
    
    -- !!! ========================================================== !!!
end

discordBtn.MouseButton1Click:Connect(function()
    if setclipboard then 
        setclipboard(discordLink)
        discordBtn.Text = "Copied Discord Link!"
        discordBtn.TextColor3 = Color3.fromRGB(100, 255, 100)
    else 
        discordBtn.Text = discordLink 
    end
    task.delay(2, function() 
        discordBtn.Text = "Join Discord"
        discordBtn.TextColor3 = Color3.fromRGB(255, 255, 255) 
    end)
end)

checkBtn.MouseButton1Click:Connect(function()
    checkBtn.Text = "Verifying with JNKie..."
    checkBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 100)
    
    local isValid, extractedKey = verifyWithJNKie(box.Text)
    
    if isValid then
        currentSettings.Key = extractedKey
        saveStuff()
        
        local dName = me.DisplayName or me.Name
        checkBtn.Text = "Welcome, " .. dName .. "!"
        checkBtn.BackgroundColor3 = Color3.fromRGB(38, 155, 65)
        task.wait(1.5)
        
        -- Set the global variable the JNKie loader is looking for!
        getgenv().SCRIPT_KEY = extractedKey
        
        -- Hide and destroy the UI
        local fi = TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.In)
        local ftw = ts:Create(keyBg, fi, {Size = UDim2.new(0, 0, 0, 0), BackgroundTransparency = 1})
        ts:Create(titleTxt, fi, {TextTransparency = 1}):Play()
        ts:Create(subTxt, fi, {TextTransparency = 1}):Play()
        ts:Create(box, fi, {BackgroundTransparency = 1, TextTransparency = 1}):Play()
        ts:Create(checkBtn, fi, {BackgroundTransparency = 1, TextTransparency = 1}):Play()
        ts:Create(discordBtn, fi, {BackgroundTransparency = 1, TextTransparency = 1}):Play()
        ts:Create(keyStroke, fi, {Transparency = 1}):Play()
        ts:Create(boxStroke, fi, {Transparency = 1}):Play()
        ftw:Play()
        ftw.Completed:Wait()
        keyScreen:Destroy()
        
        -- Finally, load the obfuscated script
        executeMainHub()
    else
        checkBtn.Text = "Invalid Key!"
        checkBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        task.wait(1.5)
        checkBtn.Text = "Verify Key"
        checkBtn.BackgroundColor3 = Color3.fromRGB(42, 78, 132)
    end
end)

-- Start the check automatically if they already saved a valid key
if currentSettings.Key and currentSettings.Key ~= "" then
    local isSavedValid, _ = verifyWithJNKie(currentSettings.Key)
    if isSavedValid then
        -- Set the variable for JNKie and bypass the UI
        getgenv().SCRIPT_KEY = currentSettings.Key
        keyScreen:Destroy()
        executeMainHub()
    else
        keyScreen.Parent = myGui
    end
else
    keyScreen.Parent = myGui
end
